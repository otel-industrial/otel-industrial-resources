# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for modbus metrics.
    type: object
    properties:
      modbus.coil.value:
        description: "ModbusCoilValueMetricConfig provides config for the modbus.coil.value metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      modbus.register.value:
        description: "ModbusRegisterValueMetricConfig provides config for the modbus.register.value metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for modbus metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
